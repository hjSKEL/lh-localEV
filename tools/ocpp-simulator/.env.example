# 실제 값은 .env 로 복사해서 채울 것 (.env는 git에 커밋하지 않음)
# TB_CHCS005.CA_ELE_NRG(시작 계량값)를 조회할 로컬 DB 접속 정보 - backup-db-copy/.env와 보통 동일함

LOCAL_DB_HOST=localhost
LOCAL_DB_PORT=3306
LOCAL_DB_USER=kevit
LOCAL_DB_PASSWORD=kevit
LOCAL_DB_DATABASE=LOCAL_CSMS
